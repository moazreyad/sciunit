name: Ubuntu

on:
  push:
  pull_request:

jobs:

  sciunit-test-with-tox:
      runs-on: ubuntu-20.04

      steps:
        - uses: actions/checkout@v1
        - name: install-tox
          run: pip3 install tox
        - name: test-with-tox
          run: tox